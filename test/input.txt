const int C1 = 5;
const bool C2 = true;

int x, y, z;
x = 10;
y = 20;
z = 30;
float a = 2.5;
bool flag = false;

if (x < 5) {
    y = y + 1;
} else if (x == 5) {
    y = y + 2;
} else {
    y = y + 3;
}

while (y < 10) {
    y = y + 1;
    if (y == 7) {
        break;
    }
    if (y == 6) {
        continue;
    }
}

for (int i = 0; i < 5; i = i + 1) {
    x = x + i;
}

repeat {
    z = z + 1;
} until (z > 10);

switch (x) {
    case 1:
        y = y + 100;
        break;
    case 2:
        y = y + 200;
        break;
    default:
        y = y + 999;
}

function int add(int a, int b) {
    int sum = a + b;
    return sum;
}

int result = add(3, 4);

x = x + (y * z) / 2 - (4 ^ 2);
flag = not flag;
